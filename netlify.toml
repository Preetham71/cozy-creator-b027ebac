[build]
  command = "bun run build"
  publish = "dist/client"

[functions]
  directory = "dist/server"

[build.environment]
  NITRO_PRESET = "netlify"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
